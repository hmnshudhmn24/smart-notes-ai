import java.util.List;

public class AITextSummarizer {
    public String summarizeText(String text) {
        // Dummy summarization logic (Replace with AI-based summarization)
        if (text.length() > 100) {
            return text.substring(0, 100) + "...";
        }
        return text;
    }
}